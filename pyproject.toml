[project]
name = "integration-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hello",
]

[tool.uv.sources]
hello = { git = "https://github.com/Harsh-2005d/hello-rust-ext.git", rev = "pip/e2e_cargo_ext" }
